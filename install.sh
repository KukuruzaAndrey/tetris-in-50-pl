# javascript
curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash -
sudo apt-get install -y nodejs

# c
# i suppose u have gcc
sudo apt install gcc

# java
sudo apt install openjdk-17-jdk openjdk-17-jre